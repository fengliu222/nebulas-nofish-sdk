<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
    <link rel="stylesheet" href="https://cdn.staticfile.org/minireset.css/0.0.2/minireset.css">
    <style>
        html,body, .container{
            height: 100%;
        }
        .container{
            margin-top: 50px;
            margin-bottom: 50px;
        }
        .header{
            width: 100%;
            height: 44px;
            position: fixed;
            top: 0;
            left: 0;
            line-height: 44px;
            padding: 0 20px;
            border-bottom: 1px solid rgba(0, 0, 0, .05);
            background-color: #f9f9f9;
            color: rgba(0, 0, 0, .85);
            font-size: 20px;
            z-index: 100;
        }
        .footer{
            z-index: 100;
            position: fixed;
            bottom: 0;
            left: 0;
            width: 100%;
            height: 44px;
            line-height: 44px;
            text-align: right;
            font-size: 12px;
            padding: 0 10px;
            color: rgba(0, 0, 0, .15);
            border-top: 1px solid rgba(0, 0, 0, .01);
            background: rgb(237,237,237);
            background: linear-gradient(180deg, rgba(237,237,237,1) 0%, rgba(215,215,215,1) 100%);
        }
        .list {
            overflow-y: scroll;
            height: 100%;
            z-index: 10;
            padding: 10px;
        }

        .list li {
            height: 60px;
            display: flex;
            align-items: center;
        }
        .title {
            flex-shrink: 0;
            margin-right: 15px;
        }
        .process {
            width: 100%;
            height: 20px;
        }
        .inner {
            line-height: 20px;
            height: 20px;
            background: #ccc;
            color: white;
            text-align: right;
            font-size: 12px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header"><%= name %></div>
        <div class="list">
            <ul>
                <% items.forEach(function(item) { %>
                    <li>
                        <div class="title">
                            <%= item.name %> 
                        </div>
                        <div class="process">
                            <div class="inner" style="width:<%= item.percent %>%">
                                <%= item.count %>
                            </div>
                        </div>
                    </li>
                <% }); %>
            </ul>
        </div>
        <div class="footer">
            powered by 无鱼排行
        </div>
    </div>
</body>
</html>